# 2026-09-01

- 完成个人工作台架构梳理：当前为 Electron 单体，main.js 同时承载桌面窗口、IPC、局域网 HTTP、权限与 JSON 数据读写；桌面端使用 IPC，共享网页使用 HTTP API。
- 建议演进为本地优先的模块化单体：Electron 仅保留原生能力，Fastify + TypeScript 提供统一 API，React/Vite 共享多端前端，SQLite（WAL）替代 JSON，未来按需迁移 PostgreSQL；实施顺序为先拆 main.js、统一 API、再迁移数据与实时同步。
- 已完成第一阶段低风险重构：main.js 收敛为生命周期和模块组装；新增 repositories/json-repository.js 统一 JSON 规范化、原子保存、自动备份和串行写队列；新增 desktop/window.js、desktop/ipc.js 与 server/http-server.js。现有数据格式、共享端路径和 API 兼容，已验证 17500 健康检查和主人任务查询返回 200。
