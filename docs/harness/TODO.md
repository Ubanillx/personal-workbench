# TODO

状态：`待办` / `进行中` / `已完成`。优先级：P0 阻断、P1 重要、P2 一般、P3 清理。

规则：一项一行；**每项必须有可验证的验收标准**；完成后写完成日期并移到底部「已完成」。

## P0 · 阻断

（当前无阻断项。原 TODO-01「建 git 仓库」已于 2026-09-10 完成，见「已完成」。）

## P1 · 重要

| ID      | 事项                                    | 验收标准                                                                                                                                                                                                                                                   | 状态   |
| ------- | --------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| TODO-02 | 解决 `.env` 不生效问题（`DEBT-02`）     | ~~二选一并落地：① 启动时加载 `.env`（Node `--env-file` 或 dotenv）；② 文档明确「必须作为真实环境变量传入」~~ **已完成**（2026-09-11）：走 ①，`loadDotEnv()` 自动加载 `.env`，`serve`/`dev` 带 `--env-file-if-exists=.env`，README 与 `.env.example` 已同步 | 已完成 |
| TODO-03 | ~~为 48 个端点补契约测试~~（`DEBT-03`） | **已完成**（2026-09-10）：改用黑盒契约快照，48 端点 × 三角色共 139 条用例，`npm run contract:compare` 全绿，正反两向自检通过；详见 `FULLSTACK_MIGRATION.md`                                                                                                | 已完成 |

## P2 · 一般

| ID      | 事项                                                            | 验收标准                                                                                                                                                                                                                                                                                                             | 状态   |
| ------- | --------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| TODO-04 | 归档死代码（`DEBT-05`）                                         | `json-migration.ts`、`json-migration.test.ts`、`task-repository.ts` 移入 `_archive/`（4 个未引用 repository 与 `owner-token.ts` 已在 B/C 阶段删除）；`typecheck` 与 `npm test` 仍全绿                                                                                                                                | 待办   |
| TODO-05 | 给「双击即启动」补一个入口（`DEBT-07`）                         | 提供一个不依赖 npm/Node 版本预检的启动方式（`.cmd` 或文档化的 `node --env-file` 命令），并在根 README 写明                                                                                                                                                                                                           | 待办   |
| TODO-06 | 备份覆盖范围与归档位置（`DEBT-08`）                             | `data/uploads/` 有备份方案；`.before-migration-*.bak` 不再散落在 `data/` 根目录                                                                                                                                                                                                                                      | 待办   |
| TODO-07 | 迁移器加内容校验和（`DEBT-09`）                                 | `schema_migrations` 增加 sql hash 列（新迁移 011），runner 启动时校验已应用迁移未被篡改                                                                                                                                                                                                                              | 待办   |
| TODO-08 | ~~拆分 `workbench.ts`~~（已并入迁移）                           | **取消**：该文件在 `FULLSTACK_MIGRATION.md` 的 Phase 2 会整体搬成框架路由，单独拆分不再有意义                                                                                                                                                                                                                        | 已取消 |
| TODO-11 | 收敛前端 effect 依赖（`DEBT-12`）                               | 先补前端测试，再开启 `react-hooks/exhaustive-deps` 与 `react/set-state-in-effect` 并修完命中；`npm run lint` 仍为 0/0                                                                                                                                                                                                | 待办   |
| TODO-12 | 补前端运行时冒烟测试（`DEBT-16`）                               | 引入 jsdom 或 Playwright，至少覆盖：应用能挂载出内容、登录页可提交、任务列表能渲染；并确认 `Select value=""`（「未分配」）的显示                                                                                                                                                                                     | 待办   |
| TODO-13 | 补齐 multipart 边界的契约用例                                   | 为「多文件 / 字段 >10 / 分片 >20 / 无文件」补 golden 用例；确认是否要对齐旧 Fastify 的 `files:1,fields:10,parts:20` 限制（当前未覆盖，行为以现实现为准）                                                                                                                                                             | 待办   |
| TODO-14 | 周报改存 WebDAV 的收尾（`REPORTS_WEBDAV.md` §14）               | 搬迁 CLI 跑完（`npm run reports:migrate-webdav`，确认没有剩余老记录）后：① 删掉 `openReportFile` 的「无前缀读本地」兼容分支；② `UPLOADS_DIR` 只服务于迁移脚本；③ 提供 `reports:migrate-webdav --pull` 反向把 NAS 文件拉回本地（回滚用）；验收：`npm test` 仍全绿（当前口径：契约 355 + test:ui 122）                 | 待办   |
| TODO-15 | 待办 / 随手记 / 重要文件的老数据归属人工确认（`D-54` + `D-55`） | 迁移 017 把历史 `todos` / `notes`、迁移 018 把历史 `important_files` 认领给本组织**最早的启用组织管理者**。上线后请该账号确认这批数据（正式库当前 8 条待办 + 1 条随手记 + 2 条重要文件）的归属是否需要改到别人名下；需要改只能走 SQL（没有转移归属的界面）；验收：确认结果记入 `ACCOUNTS_AND_ORGS.md` §23.2 与 §24.5 | 待办   |

## 迁移阶段（详见 `FULLSTACK_MIGRATION.md`）

| ID     | 阶段                       | 验收标准                                                                                                                        | 状态                 |
| ------ | -------------------------- | ------------------------------------------------------------------------------------------------------------------------------- | -------------------- |
| MIG-01 | Phase 0 契约快照           | 139 条用例录制完成；对当前实现回放 100% 一致；篡改 golden 能被抓出                                                              | 已完成（2026-09-10） |
| MIG-02 | Phase 1 数据/安全层搬迁    | 新工程用临时库启动，`/api/health`、`/api/ping` 通过 golden 比对（`/api/auth/*` 归入 Phase 2）                                   | 已完成（2026-09-10） |
| MIG-03 | Phase 2 迁移 48 端点       | 新旧实现各自 139/139 契约一致；API 层不再依赖 Fastify（物理归档归 Phase 4）                                                     | 已完成（2026-09-10） |
| MIG-04 | Phase 3 前端迁移与测试重写 | 8 个页面等价（`smoke:ui --require-migrated` 8/8 且逐页渲染出数据）；测试体系重写（db 7 + 契约 139 + 冒烟 38，`DEBT-14` 已修复） | 已完成（2026-09-10） |
| MIG-05 | Phase 4 切换与归档         | `npm start` 单进程可用；`data/workbench.sqlite` SHA-256 不变；旧实现归档可回退                                                  | 已完成（2026-09-10） |

## 账号密码 + 多组织改造（详见 `ACCOUNTS_AND_ORGS.md`）

| ID    | 阶段                        | 验收标准                                                                                     | 状态                                                                               |
| ----- | --------------------------- | -------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------- |
| ORG-A | 数据层：009/010 迁移 + CLI  | 副本上迁移后逐表比对行数一致；`user:list` / `user:passwd` / `user:init` 可用；`test:db` 全绿 | ✅ 2026-09-10（`db:rehearse` 44 项 0 失败；初始密码经独立 scrypt 复算 5/5）        |
| ORG-B | 认证：注册/登录/改密        | 注册 → 登录 → 改密 → 再登录全链路 HTTP 通过；旧令牌一律 401；`test:auth` 端到端通过          | ✅ 2026-09-10（`test:auth` 2/2；`auth.` 域 28 条契约用例）                         |
| ORG-C | 组织隔离与组织管理端点      | 用 2 个组织的夹具逐端点验证跨组织 404；§4 的 6 条不变式全部有测试                            | ✅ 2026-09-10（`org.` 域 82 条 + 主线 75 项 HTTP 探针 0 失败）                     |
| ORG-D | 界面：6 个新页面 + 8 页接入 | `smoke:ui` 全绿且渲染出真实数据；`npx antd lint app` 无问题；关键路径人工走查                | ✅ 2026-09-10（`test:ui` 79/79；antd lint 105 文件无问题；**未做浏览器人工走查**） |
| ORG-E | 契约重录与正式库迁移        | 契约 **311 条**全部一致（已重录）；正式库迁移前后行数核对；README 与 harness 六份文档回写    | ✅ 2026-09-11（正式库已应用 001–010；契约 311/311）                                |

## P3 · 清理

| ID      | 事项                                   | 验收标准                                                                                                                                                        | 状态                 |
| ------- | -------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------- |
| TODO-09 | 删除空目录                             | 已删除 `server/src/middleware/`、`server/src/utils/`、`test/unit/`、`test/fixtures/`（含 `templates/`）；`web/src/components/` 随 `web/` 归档；全仓复查无空目录 | 已完成（2026-09-10） |
| TODO-10 | 决定是否走 Go 重写（`PLAN.md` 路线 B） | 已在 `PLAN.md` 决策记录 D-05 写明结论：**放弃 Go 重写**                                                                                                         | 已完成（2026-09-10） |

## 已完成

| ID        | 事项                                                          | 完成日期   | 结果                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| --------- | ------------------------------------------------------------- | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| MIG-01…05 | 全栈迁移 Phase 0–4（Fastify SPA+API → React Router 8 单进程） | 2026-09-10 | 139 条契约快照 → 48 端点迁移 → 8 页面前端迁移 → 切换与归档。旧实现 20 文件归档到 `_archive/legacy-fastify/`；移除 6 个 fastify 包 + concurrently（84 个包）；lint 0/0、typecheck 0、测试 185 项全绿、`format:check` 与 `antd lint app` 无漂移；正式库 SHA-256 前后一致；`npm start` 单进程跑在 `127.0.0.1:17500`。详见 `FULLSTACK_MIGRATION.md`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| —         | 修复 `npm start` 的端口/绑定语义缺陷                          | 2026-09-10 | `react-router-serve` 无 `PORT` 时默认 3000、被占用时静默换随机端口，无 `HOST` 时绑定所有网卡；`npm start`/`start:lan` 已显式固定 `HOST`+`PORT=17500`，README 加警告                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| —         | Electron 遗留栈清理（代码 / bat / ps1 / 文档 / 旧 JSON 数据） | 2026-09-10 | 45 项移入 `_archive/legacy-electron/`；`electron` 依赖卸载（node_modules 389 MB → 116 MB）；`owner:reset`、`db:backup` 迁至 `server/src/cli/` 且命令名不变                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| —         | 删除 `db/` 空目录与 5 个孤儿数据/夹具文件                     | 2026-09-10 | 正式库 SHA-256 前后一致，`npm test` 16/16                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| TODO-01   | 建立 git 仓库并做基线快照                                     | 2026-09-10 | `git init -b main`（a9fa96c 基线）；`.gitignore` 排除 `data/`、`_archive/legacy-electron/data/`（内含明文令牌）、构建产物；首提交跟踪 104 个文件，无隐私文件入库                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| —         | 周报/总结正文改存 NAS（D-46）                                 | 2026-09-12 | 归属人固定为登录账号（代传取消、管理员不提交）；正文只写 NAS（`<上传根>/<用户名>/<起止日期>/<文件名>`，重传 `_v{n}`、撞名 `_2`、绝不覆盖）；管理员在 `/settings → WebDAV → 周报上传` 配统一账号；下载改服务端流式代理；新增迁移 014 与搬迁 CLI `reports:migrate-webdav`（`--dry-run` / `--purge`）。验收：契约 317→318、测试 429→448 全绿、六项门禁全绿。详见 `REPORTS_WEBDAV.md`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| —         | 重要文件路径改为选（D-48）                                    | 2026-09-12 | 新建/编辑抽屉的「文件路径」改为「选择文件」：用面包屑 + 本目录筛选浏览远端，选中即填 `webdav:<相对路径>`（名称空着自动填、保存才落库），编辑定位到所在目录并标「当前」；字段只读、只能选（本机路径不再新增）；「选择文件」与「浏览 WebDAV」共用 `webdav-browser.tsx`；冒烟补 5 项 WebDAV 通道开/关断言。验收：契约 318、test:ui 90→95、全量测试 448→453 全绿、lint/typecheck/antd lint 全绿。详见 `WEBDAV.md` §2.2                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| —         | 重要文件支持远端下载（D-49）                                  | 2026-09-12 | 新增 `GET /api/files/:id/download`：按索引 id 流式代理 NAS 文件（`client.open` + `content-disposition`，远端真实文件名），边界与文件库一致（requireManager + 组织隔离 + 本机路径 400 + 未配置 503）；列表远端条目带「下载」（远端已不存在置灰，本机条目无入口）；夹具补一条 `webdav:` 索引 + 契约 `files.download.*` 6 条 + 冒烟「上传 → 登记 → 下载」闭环。验收：契约 318→324、test:ui 95→99、全量测试 453→463 全绿、lint/typecheck/antd lint 全绿。详见 `WEBDAV.md` §4 第 13 条                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| —         | 周报上传共用管理员连接（D-52）                                | 2026-09-12 | 周报正文不再单独养连接：凭据取 `webdav_settings` 里管理员那一行（多管理员取最近保存的），「周报上传」卡只剩「上传根目录」，默认**跟随那份连接的浏览根目录**；没有 `report_upload_settings` 行不再是「未配置」；`clear-report-upload` → `reset-report-upload`；迁移 015 重建单行表（老行的 `/周报` 原样保留）。验收：契约 324 全一致（golden 重录，`request.path` 一并归一化）、test:db 31、test:ui 99 全绿、六项门禁全绿。详见 `REPORTS_WEBDAV.md` §15                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| —         | 周报上传目录按组织（D-53）                                    | 2026-09-12 | 上传根目录从「全局一份」改成「一个组织一行」（迁移 016：主键 `id=1` → `org_id`，老的全局目录**复制给每个组织**，升级后落点不变）；配置权下放给**该组织的管理者**（`assertOrgManage`；管理员可在卡里切换组织，管理者提交的 `orgId` 一律忽略）；读目录变成 `reportUploadRootFor(orgId)`（上传按周报所属组织、下载不受影响）；连接仍是管理员那份（D-52）；没配目录 = 用连接的浏览根。验收：契约 324 全一致（阿尔法 `/阿尔法/…`、贝塔 `/…`）、test:db 31、test:ui 99→107 全绿、六项门禁全绿。详见 `REPORTS_WEBDAV.md` §16                                                                                                                                                                                                                                                                                                                                                                                                 |
| —         | 权限规则调整（D-54）                                          | 2026-09-14 | ① 私密任务可见方收敛为**发布人 / 负责人 / 全局管理员**（组织管理者不再因角色看到他人的私密任务；D-36「负责人必须是创建者本人」取消，负责人可指派给本组织他人）；② 普通成员可看本组织全部非私密任务（页面同步**不渲染**「编辑/改派」，而不是渲染成灰按钮）；③ 待办与随手记改为**本人数据**（迁移 017 加 `owner_id`，管理员也只看自己的；正式库 8 条待办 + 1 条随手记认领给组织管理者）；④ 重要文件查看/新增/编辑对组织内所有人开放、**删除只给管理者**（顺带补上此前根本不存在的 `PATCH /api/files/:id`）；⑤ 周报组织内全可见、只有重传与审批分角色。**同时堵掉一处真实裂缝**：写操作此前只判角色，组织管理者能改自己看不见的私密任务（现在读写共用同一个 `canView`）。验收：`npm test` 全绿（db 31 · 契约 **355/355** · auth 2 · ui **122/122**）、迁移演练在正式库副本上 8/8、lint 0/0（159 文件）、typecheck 四工程 0、`format:check` 无漂移。详见 `ACCOUNTS_AND_ORGS.md` §23                                       |
| —         | 重要文件按条选可见范围（D-55）                                | 2026-09-14 | 重要文件此前是「本组织全可见」一刀切，个人用的报价底稿一登记就进了全组织视图。改后**每条索引自带 `visibility`**（迁移 018 加 `visibility` + `owner_id`，**默认组织可见**、老数据回填为 `org`，行为零变化）：`private` = **创建人 + 本组织的全局管理员**看得到，**可见即可改可删**、**归属不可转让**、跨组织仍 404；删除随之改为**逐条判**（创建人可删自己的含个人文件，组织管理者可删本组织的组织文件，管理员全可删）。三个新建入口（添加文件 / 选择文件后保存 / 浏览 WebDAV 的选中即登记）都能选，列表加「可见范围」列与逐条删除入口。验收：`npm test` 全绿（db 31 · 契约 **369/369** · auth 2 · ui **128/128**）、迁移演练在正式库副本上 **11/11**（含「老数据没有被标成 private」这条行为零变化断言）、lint 0/0、typecheck 四工程 0、`format:check` 无漂移。详见 `ACCOUNTS_AND_ORGS.md` §24                                                                                                                        |
| —         | 任务验收权归发布人（D-57）                                    | 2026-09-14 | 起因：进度到 100% 提交验收后，**任务执行者自己也能点「通过验收」**——`approveTask` / `returnTask` 当时只判角色（admin/manager），既不看发布人也不看负责人，同组织的任何管理者都能验收别人的任务。改后**验收权归任务的发布人**（`created_by`，全局管理员兜底），**执行者永不验收自己负责的任务**；判据抽成前后端共用的 `app/lib/task-permissions.ts` `canReviewTask()`（页面不满足就整块不渲染按钮，与服务端同一份判据）。顺带对齐状态机：100% 是否进待验收改判「有没有第二方」（`created_by` 是不是自己，管理者自己负责的任务不再直接完成），已完成任务不能再 `submit-review` 拉回待验收（列表按钮同步禁用），「待验收」通知补上发布人（管理员发布的组织任务原本没人收得到）。验收：`npm test` 全绿（db 31 · webdav 13 · 契约 369→**378/378**，golden 已重录 · auth 2 · ui 138）、lint 0/0（163 文件）、typecheck 四工程 0、`npx antd lint app` 119 文件无问题、`format:check` 无漂移。详见 `ACCOUNTS_AND_ORGS.md` §25 |
