# 周报/总结正文改存 WebDAV（归属人固定为本人）

状态：**已实现（2026-09-12，D-46）**；配置口径同日先按 **D-52** 改成「共用管理员那份连接」，
再按 **D-53** 改成「**上传目录按组织配置**，组织管理者可改本组织」（见 §15、§16）。
本文是这条链路的唯一权威口径；实施结果与偏差见 §14、§15、§16。

关联：[`WEBDAV.md`](WEBDAV.md)（远端接入层：客户端、按账号配置、路径归一化）、
[`ACCOUNTS_AND_ORGS.md`](ACCOUNTS_AND_ORGS.md)（角色与组织隔离）、[`DATA_MODEL.md`](DATA_MODEL.md)（表结构与迁移机制）。

> §1–§13 是**设计冻结件**（两轮问答确认的取舍，实施时照着做）；§14 记录实际做出来的是什么、
> 与设计有哪些偏差；§15 / §16 记录 D-52 / D-53 对**配置口径**的后续改动。
> 改这条链路前先读 §2 的冻结点、§14 的偏差清单与 §16。
>
> **D-52 + D-53 合起来改掉的只有「连接从哪来、目录写哪里」**：周报正文不再单独养一个账号，
> 而是**共用管理员在「WebDAV 连接」里保存的那份**；上传根目录**按组织**分开配，
> 组织管理者配本组织（管理员可配任意组织），**默认**用那份连接的浏览根目录。
> 落点命名、撞名规则、下载代理、迁移 CLI 全部不变（沿用 D-46）。

## 1. 要解决什么

| 维度         | 现在                                                                                    | 改后                                                                                                           |
| ------------ | --------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------- |
| 谁提交       | member 只能传自己的；manager / admin 要在表单里选「归属人」代传（admin 还可跨组织代传） | **谁登录谁就是归属人**；「归属人」字段整体消失，代传取消，admin 不提交                                         |
| 正文存哪     | 服务器本地 `data/uploads/reports/<reportId>/v<N>.<ext>`                                 | **只存 NAS**：`<上传根目录>/<登录用户名>/<起止日期>/<文件名>`                                                  |
| 谁能看到正文 | 只有装了本服务、且能登录本服务的人                                                      | NAS 上有该共享目录权限的人（代价见 §11）                                                                       |
| 备份         | `DEBT-08`：`data/uploads/` 不在任何备份范围，DB 有元数据、文件丢了就没了                | 由 NAS 的快照 / 回收站负责；本机不再存正文                                                                     |
| 配置         | 无（本地目录由 `UPLOADS_DIR` 写死）                                                     | 复用管理员那份 WebDAV 连接（D-52）；上传根目录**按组织**配（D-53，组织管理者维护，默认用连接的浏览根目录，§4） |
| 下载         | `fsp.readFile` 全量读本地再回传                                                         | 服务端**流式代理** `/api/reports/:id/file/:version` → NAS（顺带修掉全量读）                                    |

一句话：**谁登录谁就是归属人；正文只存 NAS，路径按「归属人 / 周期 / 文件名」。**

## 2. 冻结点（全部已确认）

| #   | 冻结点                                                                                                                                                                  |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | 归属人 = 登录账号本身，上传表单不再有「归属人」                                                                                                                         |
| 2   | 能提交的只有 member / manager，且只能提交**自己的**（`owner_id` = 自己，`org_id` = 自己组织）                                                                           |
| 3   | **admin 不能提交**：`/reports` 上传入口对他隐藏，API 直接 403                                                                                                           |
| 4   | 代传彻底取消；审批 / 退回 / 通知 / 组织隔离口径不变（跨组织仍 404）                                                                                                     |
| 5   | 正文**只写 NAS，本地不留副本**                                                                                                                                          |
| 6   | 落点 `<上传根目录>/<用户名>/<period_start>_<period_end>/<原文件名>`                                                                                                     |
| 7   | 归属人目录用**登录用户名**（不用显示姓名）                                                                                                                              |
| 8   | 周期目录用**起止日期**：`2026-09-01_2026-09-07`                                                                                                                         |
| 9   | 文件名沿用**上传的原文件名**                                                                                                                                            |
| 10  | 退回重传（同一条周报的新版本）→ 追加 `_v2` / `_v3`                                                                                                                      |
| 11  | 同人 + 同周期 + 同文件名**再新建**一份 → 自动 `_2` / `_3`，**绝不覆盖**                                                                                                 |
| 12  | ~~统一上传账号（用户名 / 密码）由**管理员**在 `/settings → WebDAV` 维护，地址仍用 `.env` 的 `WEBDAV_URL`~~ → **D-52 改为：共用管理员在「WebDAV 连接」里保存的那份连接** |
| 13  | 上传根目录只有管理员能改；~~全局一份、默认 `/周报`~~ → **D-52：默认跟随共用连接的浏览根目录，可另挑一个**                                                               |
| 14  | 现有**按账号**的 WebDAV 配置**保留**，继续服务「重要文件」，两者互不干扰                                                                                                |
| 15  | 下载走**服务端流式代理**，应用层权限判定照旧，NAS 凭据不出服务器                                                                                                        |
| 16  | NAS 层不做隔离（共用一个 NAS 账号写入 = NAS 上人人可翻），靠 NAS 自己的共享权限管 —— 已确认接受                                                                         |
| 17  | 存量正文用**一次性 CLI** 全部搬到 NAS 并回写索引；默认不删本地，`--purge` 才删                                                                                          |

约束不变：单文件 ≤ 20 MB；扩展名只允许 `.xlsx / .xls / .docx / .doc`；`report_files` 的版本语义、审批状态机不动。

## 3. 落点与命名

### 3.1 路径规则

```text
{上传根目录}/{username}/{periodStart}_{periodEnd}/{文件名}
```

以根目录 `/周报`、账号 `zhangsan`、周期 `2026-09-01 ~ 2026-09-07` 为例：

| 场景                             | 远端路径                                                  |
| -------------------------------- | --------------------------------------------------------- |
| 新建提交 `第八周周报.docx`       | `/周报/zhangsan/2026-09-01_2026-09-07/第八周周报.docx`    |
| 被退回后重传（库里 version = 2） | `/周报/zhangsan/2026-09-01_2026-09-07/第八周周报_v2.docx` |
| 同期同人再新建一份同名文件       | `/周报/zhangsan/2026-09-01_2026-09-07/第八周周报_2.docx`  |

`periodStart` / `periodEnd` 直接取周报表里的 `YYYY-MM-DD` 值（表单 RangePicker 的产出），不再二次换算周号。

**「上传根目录」默认是什么**：D-52 起默认**跟随共用连接的浏览根目录**（`report_upload_settings` 没有这一行，
或 `root` 是空串）。连接根是 `/` 时，上表里的 `/周报` 就整个消失，落点变成 `/zhangsan/2026-09-01_…/周报.docx`；
管理员另外挑过目录（例如 `/周报`）时，上表原样成立。

### 3.2 名字清洗（三段都要过）

复用现有 `reports.server.ts` 的 `sanitizeName` 口径，并补齐目录段规则：

- 剔除控制字符与 `/`、`\`；去掉首尾空格与首尾 `.`（避免 `..` 与前导点）；
- 空段回退 `unnamed`；单段上限 120 字符，超出截断但保留扩展名；
- 用户名取 `users.username`（注册已限字符集），**仍然按同一规则清洗**——库里出现异常值时不能把路径顶穿；
- 扩展名白名单与大小限制不变，文件名主体保留原大小写。

### 3.3 撞名与版本（远端绝不静默覆盖）

上传前先决定目标名，再用 **`PUT` + `If-None-Match: *`** 让 NAS 端原子拒绝覆盖（存在则 `412`）：

| 场景        | 目标名             | 已存在时的处理                                    |
| ----------- | ------------------ | ------------------------------------------------- |
| 新建（v1）  | `{stem}{ext}`      | `{stem}_2{ext}`、`{stem}_3{ext}` … 最多试探 20 次 |
| 重传（v≥2） | `{stem}_v{n}{ext}` | `{stem}_v{n}_2{ext}`、`_v{n}_3{ext}` … 同样 20 次 |

- NAS 不支持 `If-None-Match`（返回 501 或忽略了条件头）时，回退到 `PROPFIND Depth:0` 探测存在性；
- 试探到上限 → `409 CONFLICT`「远端已存在同名文件，请改名或换一个周期」；
- **库里的 `report_files.version` 是权威**，文件名后缀只是给人看的，因此出现 `_v2_2` 属于正常结果；
- 这条规则取代了本地时代的 `flag: "wx"`（独占创建）语义。

### 3.4 `report_files.stored_name` 的新值域

| 取值                                                          | 含义                                                       |
| ------------------------------------------------------------- | ---------------------------------------------------------- |
| `webdav:/周报/zhangsan/2026-09-01_2026-09-07/第八周周报.docx` | **新式**：远端路径（相对 **WebDAV 服务根**，含上传根目录） |
| `v1.xlsx`                                                     | **旧式**：本地文件名（迁移脚本搬完并回写后不再产生）       |

三个要点：

1. 前缀复用 `shared/types/domain.ts` 的 `WEBDAV_PATH_PREFIX`（`webdav:`），与 `important_files` 的约定同源；
2. 存的是**含上传根目录的完整相对路径**，而不是「相对上传根」——这样管理员日后改根目录，老记录仍能下载（代价见 §11.3）；
3. 文件名列不加、表不加，只换 `stored_name` 的值域，**迁移 014 不动 `report_files`**。

## 4. 配置：共用连接 + 每个组织一个上传目录（D-53 口径；D-46 / D-52 原文见 §15.2）

### 4.1 数据

迁移 `016_report_upload_settings_per_org.sql`（在 `015` 的形状上重建，主键从「单行 id=1」改成 `org_id`）：

```sql
CREATE TABLE report_upload_settings (
  org_id     TEXT PRIMARY KEY REFERENCES organizations(id) ON DELETE CASCADE,  -- 一个组织最多一行
  root       TEXT NOT NULL DEFAULT '',        -- 空串 = 用连接的浏览根目录
  updated_by TEXT REFERENCES users(id) ON DELETE SET NULL,
  updated_at TEXT NOT NULL
);
```

- **连接不在这张表里**：它取 `webdav_settings` 里 `role='admin'` 的那一行（多个管理员取最近保存的），
  见 `webdav-settings.server.ts` 的 `sharedWebDavConnection()`；地址仍是部署级的 `WEBDAV_URL`（D-44 不变）；
- **目录按组织**：`reportUploadRootFor(orgId)` 取本组织那一行；**没有这一行 = 用连接的浏览根目录**，
  所以「没配」不是失败态，成员照常能交周报（能交不能交只看「地址配好 **且** 有管理员保存过连接」）；
- 组织之间的差异**只有目录**：文件始终用管理员那个 NAS 账号写（D-52 不变）——
  各组织写进 NAS 上不同的目录，而不是各配一套凭据；
- `webdav_settings`（按账号）**原样保留**，继续给「重要文件」用；管理员的同一份被周报共用。

### 4.2 设置页

`/settings → WebDAV` Tab 内的「周报上传」区块，**作用域是一个组织**：

| 字段       | 必填 | 默认                   | 说明                                                                      |
| ---------- | ---- | ---------------------- | ------------------------------------------------------------------------- |
| 组织       | —    | 当前管理组织           | 管理员是下拉框（可切换，挂在 `?org=` 上）；组织管理者是本组织的只读展示   |
| 上传根目录 | 否   | **用连接的浏览根目录** | 复用「选择目录」Drawer（`webdav-dir-picker.tsx`）挑；留空即用连接的浏览根 |

- **谁能改**：`assertOrgManage`——管理员可改任意组织，**组织管理者只能改本组织**
  （组织管理者提交的 `orgId` 一律被忽略，用会话里的组织，与其它设置动作同一口径）；
- **没有用户名 / 密码 / 超时**：它们全来自那份共用连接——同一台 NAS 只填一遍账号（D-52 的动机）；
- **也没有连接信息的只读回显**：连接就显示在正上方那张「WebDAV 连接」卡里，
  在自己的卡里再画一遍属于信息层级错误（同一个 Tab 里同一样东西只出现一次）；
  真的没有可用连接时，用一条 Alert 说明「先去上面保存一次」就够了。

action：`save-report-upload` / `reset-report-upload` / `test-report-upload` / `browse-report-upload`
（`reset` 只删**本组织**那一行，回到默认目录，**不会**让周报上传失效；
`test` / `browse` 是连接级的，用**服务端的共用连接**去连，不读表单凭据）。

## 5. 接口与权限变化

### 5.1 `POST /api/reports`（创建并提交）

| 字段                          | 现在                     | 改后                               |
| ----------------------------- | ------------------------ | ---------------------------------- |
| `ownerId`                     | member 之外**必填**      | **不再读取**（传了也忽略，不报错） |
| `periodStart` / `periodEnd`   | 必填                     | 不变                               |
| `docType`                     | 必填                     | 不变                               |
| `note`                        | 选填                     | 不变                               |
| 文件分片                      | 必填，≤20 MB，白名单扩展 | 不变（改为 PUT 到 NAS）            |
| 响应 `data.ownerId/ownerName` | 归属人                   | 恒为**提交者自己**                 |

角色矩阵：

| 角色    | 现在                                  | 改后               | 文案 / 状态                                                    |
| ------- | ------------------------------------- | ------------------ | -------------------------------------------------------------- |
| member  | 只能提交自己的                        | 不变               | —                                                              |
| manager | 自己的 + 代传本组织成员               | **只能提交自己的** | 代传语义消失（`ownerId` 被忽略）                               |
| admin   | 可代传任意组织（必须显式指定归属人）  | **禁止提交**       | 新文案 `FORBIDDEN` 403：「管理员不提交周报，请由成员本人提交」 |
| 无组织  | 403「你还没有加入组织，无法提交周报」 | 不变               | —                                                              |

`POST /api/reports/:id/file`（退回后重传）权限判定不变（本人或本组织 manager / admin），只把落盘换成 NAS + `_v{n}` 命名。

### 5.2 `GET /api/reports/:id/file/:version`（下载）

顺序：

1. `getReportFile` 的权限与组织边界判定**完全不变**（跨组织 / 看不到的一律 404）；
2. `stored_name` **无** `webdav:` 前缀 → 走旧的本地读路径（兼容期，见 §6.3）；
3. 有前缀 → 用**共用连接**（管理员那份）向 NAS `GET`，`Readable.fromWeb(...)` **流式**回传；
4. 响应头保持 `content-type`（库里 `mime_type`，缺失时按扩展名推）、
   `content-disposition: attachment; filename*=UTF-8''<原文件名>`、`content-length`（库里 `size_bytes`，未知则不设）；
5. 远端 404 → `NOT_FOUND`「文件已丢失」404（沿用现文案）；
6. 没有可用连接（没配 `WEBDAV_URL` / 没有管理员保存过连接）→ 503 `WEBDAV_DISABLED`，文案：「周报存储未配置：请联系管理员在「设置 → WebDAV」保存一次连接」。

> 现有 `WEBDAV_DISABLED` 文案（「未配置 WebDAV：请到「设置 → WebDAV」填写地址与账号」）是给「重要文件」按账号配置用的，
> 两者受众不同：周报的读者是**普通成员**，不该被指去一个他们进不去的设置页，因此新文案单独一条。

### 5.3 页面 `/reports`

- 上传按钮 / 抽屉：**admin 不渲染**（列表、筛选、审批、退回、下载照旧）；
- 抽屉里删掉「归属人」表单项，保留周期 RangePicker、类型、备注、文档；
- 列表「归属人」列保留（现在恒为提交者本人，manager / admin 看到的仍是各成员自己的）；
- 上传失败（NAS 不可达 / 409 撞名上限 / 503 未配置）在抽屉内提示并允许重试，不吞错。

### 5.4 其它端点

`GET /api/reports`、`GET /api/reports/:id`、`approve`、`return` 的**请求与响应逐字段不变**（契约无 delta）。

## 6. 数据与迁移

### 6.1 迁移清单

| 编号  | 内容                                                        | 影响面                                |
| ----- | ----------------------------------------------------------- | ------------------------------------- |
| `014` | 建 `report_upload_settings`（单行，见 §4.1）                | 新表；不动任何既有表 / 列 / 数据      |
| `015` | 重建 `report_upload_settings`：只剩 `root`（D-52，见 §4.1） | 重建同一张表；老行的 `/周报` 原样保留 |

`report_files` **不加列、不改结构**，只换 `stored_name` 的值域（§3.4）。

### 6.2 存量搬迁 CLI

```bash
npm run reports:migrate-webdav -- --dry-run     # 只打印计划（先跑这个）
npm run reports:migrate-webdav                  # 上传 + 回写 stored_name
npm run reports:migrate-webdav -- --purge       # 收尾：删掉本地正文与空目录
```

- 逐条处理 `report_files`：**无前缀**的记录读本地 `data/uploads/reports/<reportId>/<stored_name>`，
  按 §3.1 的新命名（版本对齐库里 `version`：v1 不加后缀，v≥2 加 `_v{n}`）PUT 到 NAS，成功后回写 `stored_name`；
- **幂等**：已有 `webdav:` 前缀的记录直接跳过，可反复跑；
- `--purge` 删除本地文件与 `data/uploads/reports/<reportId>/` 空目录；**删前必须再 `stat` 一次远端确认存在**；
- 缺本地文件、NAS 不可达、目标已存在 → 逐条记失败并汇总，**退出码非 0**，不半途而废、不静默跳过；
- 没有可用连接（没配 `WEBDAV_URL` / 没有管理员保存过连接）→ 直接拒绝运行并提示先去设置页保存连接。

### 6.3 兼容期：双读

无前缀 = 本地（老记录），有前缀 = NAS（新记录）。这个过渡态有**两个不可省的用处**：

1. 迁移脚本跑之前系统照常可用，不需要「停机搬文件」；
2. **契约夹具与 SSR 冒烟不依赖外部 NAS**（见 §8）——它们跑的就是旧式记录。

兼容期何时结束：等 CLI 跑完、并确认没有无前缀记录（CLI 会打印剩余清单）后，本地读路径可以整体删除。
把「删掉本地读分支 + `UPLOADS_DIR` 只服务于迁移脚本」记为一次独立的小提交。

## 7. 失败与降级速查

| 场景                         | 表现                                                                       |
| ---------------------------- | -------------------------------------------------------------------------- |
| `.env` 没配 `WEBDAV_URL`     | 设置页提示地址未配置；上传 / 下载（新式）503；列表与审批照旧能用           |
| 没有管理员保存过 WebDAV 连接 | 同上（503 +「周报存储未配置」）；成员不需要做任何事，等管理员保存即可      |
| **本组织没配上传目录**       | **不是失败态**：用连接的浏览根目录，照常能交周报（组织管理者可随时挑一个） |
| NAS 不可达 / 超时            | 上传失败并提示「WebDAV 暂时不可达」+ 可重试；列表与审批不受影响            |
| 远端文件被人手工删了         | 下载 404「文件已丢失」                                                     |
| 撞名试探到上限               | 409「远端已存在同名文件，请改名或换一个周期」                              |
| 远端 PUT 成功、回写 DB 失败  | 远端留下**孤儿文件**（不做自动清理，记入 §11.5）                           |
| 超 20 MB / 非法扩展名        | 413 / 400，文案不变                                                        |

## 8. 契约与测试影响

当前 golden 实际是 **324 条**（`test/contract/golden/contract.golden.json` 的 `meta.caseCount`；
D-46 实施时为 318，`docs/harness/README.md` 与 `PLAN.md` 里写的 311 是 D-47 之前的旧数字）。

> **D-52 / D-53 的契约 delta**（用例条数不变，仍是 324）：夹具给**管理员**预置了 `webdav_settings`
> （周报上传要用它），于是 `webdav.list.admin.disabled` 变成 `webdav.list.admin.missing-dir`
> （200 + 空列表，不再是 503；管理者/成员的 503 两条例照旧）；夹具再给**阿尔法组**配了
> `report_upload_settings.root = '/阿尔法'`、贝塔组**不配**，于是周报类用例的 `storedName` 一眼能看出
> 两个组织写在不同目录：阿尔法是 `webdav:/阿尔法/member-a/…`，贝塔是 `webdav:/member-b/…`（连接根）。
> 顺带把 `request.path` 也纳入 `normalizeValue`（uuid → `<uuid>`），重录 golden 不再漂 uuid 噪音。

**（D-46 原文）周报域 36 条里，delta 如下：**

| 用例                                      | 现在                        | 改后                                           |
| ----------------------------------------- | --------------------------- | ---------------------------------------------- |
| `reports.create.memberA.upload`           | 201                         | 201（载荷不变，正文改落 NAS）                  |
| `reports.create.memberA.other-owner`      | 201                         | 201（`ownerId` 被忽略，语义变化、载荷不变）    |
| `reports.create.managerA.proxy`           | 201 代传                    | 201，但 `ownerId/ownerName` 变成 managerA 自己 |
| `reports.create.managerA.cross-org-owner` | 400「归属人必须属于本组织」 | **201**，归属 managerA 自己（文案与状态都变）  |
| `reports.upload-new-version`              | 201                         | 201（落 NAS，`_v2` 后缀）                      |
| `reports.download.uploaded` / `-v2`       | 200                         | 200（改从 NAS 读；夹具走旧式记录时仍读本地）   |
| **新增** `reports.create.admin.forbidden` | —                           | 403 + 新文案                                   |

其它测试：

| 项            | 变化                                                                                                                                              |
| ------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| `test:webdav` | 9 → 约 15 项：新增 GET 流式下载、`If-None-Match` 412 与探测回退、后缀递增、路径清洗与上限                                                         |
| `test:db`     | 20 → +2：`report_upload_settings` 读写、搬迁脚本的幂等 / 失败汇总                                                                                 |
| `test:ui`     | 81 → +2：`/reports` 的标记串按身份断言（member/manager 有「上传周报」，admin **没有**）；`/settings?tab=webdav` 断言「周报上传」区块仅 admin 可见 |
| 门禁          | `npm run lint` / `typecheck` / `npm test` / `build` / `format:check` / `npx antd lint app` 全绿                                                   |

## 9. 实施顺序与工作量

| 步  | 内容                                                                                                                                          | 估时         |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------- | ------------ |
| 1   | 迁移 `014` + `webdav-settings.server.ts` 扩展（单行配置 CRUD / 视图 / 校验）                                                                  | 0.5 天       |
| 2   | `server/src/webdav/client.ts` 加 `PUT If-None-Match` 与 `GET` 流式下载；`app/lib/report-storage.server.ts`（新）承载命名 / 撞名 / 上传 / 下载 | 0.5 天       |
| 3   | `reports.server.ts` 改写 NAS（create / reupload / getReportFile）+ 下载路由改流式                                                             | 0.5 天       |
| 4   | 设置页「周报上传」区块（复用目录选择器）+ `/reports` 页面改动                                                                                 | 0.5 天       |
| 5   | 存量搬迁 CLI `tools/reports-migrate-webdav.ts` + npm script                                                                                   | 0.5 天       |
| 6   | 契约用例调整 + 假 WebDAV 抽公用 + 重录 golden（317 条）                                                                                       | 0.5 天       |
| 7   | 文档回写（本文转「已实现」+ `WEBDAV/ARCHITECTURE/DATA_MODEL/README/TECH_DEBT`）+ 全量门禁                                                     | 0.5 天       |
|     | **合计**                                                                                                                                      | **≈ 3.5 天** |

## 10. 契约环境里的「远端」：已定为方案 A

上传类用例（`reports.create.*`、`reports.upload-new-version`）落 NAS 之后，没有任何远端可写的契约环境会整体 503。
**已按方案 A 落地**：

| 方案                                              | 说明                                                                                                                                                                 | 结论       |
| ------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| **A. 抽一个公用假 WebDAV**                        | 把 `test/webdav/client.test.ts` 里的内存假服务器抽成公用模块，`contract:capture/compare` 与 `smoke:ui` 拉起它并把 `WEBDAV_URL` 指过去；上传 / 下载用例保持**真链路** | **已采用** |
| B. 上传类用例退出 golden，改由 `test:webdav` 覆盖 | 省事，但契约里出现**空洞**（`POST /api/reports` 不再有黑盒快照）                                                                                                     | 未采用     |
| C. 给契约夹具开一条「本地落盘」后门               | 与「只存 WebDAV」直接冲突，双实现长期维护                                                                                                                            | 不推荐     |

## 11. 风险与取舍

1. **NAS 层没有隔离**（已确认接受）：所有组织写进 NAS 上（各自的）目录，能打开那些共享的人可以看到所有人的周报——
   应用层的组织隔离（跨组织 404）仍然生效，但它管不到 NAS 界面。要收紧只能靠 NAS 自己的共享权限。
2. **提交强依赖 NAS**（已确认接受）：NAS 挂了 / 网络不通就交不了周报，没有本地兜底。
   这正是「只存 WebDAV」的直接代价，换取的是「文件天然在备份体系里、人在 NAS 上直接能翻」。
3. **存的是含根目录的完整路径**：改「上传根目录」只影响新文件，老记录仍指向旧目录。
   好处是老周报不会因为改配置而失效；代价是 NAS 上会同时存在旧根与新根两个时期的内容——**目录定了就别改**。
   （D-52 之后「默认目录」是**动态**的：没另外挑过时跟随连接的浏览根，改连接根会让**新文件**换地方，老记录照旧。）
4. **代理下载占应用带宽**：浏览器流量经应用转发到 NAS（换来的是权限判定与凭据不出服务器）。
   实现改为流式后内存占用与文件大小无关（顺带修掉现在「全量 `readFile`」的问题）。
5. **孤儿文件**：远端 PUT 成功但 DB 回写失败时，NAS 上会留一份没人知道索引的文件。不做自动清理，
   排查办法见 §6.3 的剩余清单与 CLI 输出。
6. **`DEBT-08` 部分关闭**：周报正文不再只存在本机（NAS 侧由 NAS 的快照 / 回收站负责），
   但 `.before-migration-*.bak` 散落问题仍在，`DEBT-08` 保留并把「周报文件无备份」一句划掉。

## 12. 回滚

- **迁移前**：`npm run db:backup`（数据库快照）+ 不跑 `--purge`（本地正文原样留着）；
- **代码回滚**：老记录（无前缀）照旧读本地，功能不受影响；新记录（有前缀）在回滚后的版本里下不了
  （远端文件仍在 NAS，可用 NAS 界面取），列表与审批照旧；
- **完全回退**：恢复备份库 + 反向把 NAS 文件拉回本地（脚本可后补 `--pull` 模式，记入 `TODO.md`，
  本次不做）。

## 13. 决策记录

| ID     | 决策                                                                                                          | 理由                                                                                                    |
| ------ | ------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------- |
| D-46   | **周报/总结正文改存 WebDAV，归属人固定为登录账号本人**（见 §2 的 17 条冻结点）                                | 汇报文档天然属于「人 + 周期」，路径按人可读；正文进 NAS 后天然进入备份体系，本机不再持有唯一副本        |
| D-46.1 | 只存 NAS，本地不留副本                                                                                        | 「本机不再是唯一副本」是这次改造的主要收益                                                              |
| D-46.2 | ~~上传位置与统一账号由管理员在 `/settings → WebDAV` 维护，全局一份，根目录默认 `/周报`~~ **（被 D-52 取代）** | 成员不该为了交周报去碰 NAS 配置；地址仍走 `.env`（D-44 口径不变）                                       |
| D-46.3 | 命名 `上传根/用户名/起止日期/原文件名`；重传 `_v{n}`，撞名 `_2/_3`，远端绝不覆盖                              | 人对 NAS 的直觉就是按人按周找文件；覆盖会造成不可逆的数据丢失                                           |
| D-46.4 | 下载走服务端流式代理，不用 302 直链                                                                           | 保住应用层权限判定，NAS 凭据不出服务器                                                                  |
| D-46.5 | 存量用一次性 CLI 搬迁，默认不删本地；兼容期双读（无前缀 = 本地）                                              | 不用停机搬文件；契约与冒烟不依赖外部 NAS                                                                |
| D-52   | **周报上传不再单独养一个连接：共用管理员在「WebDAV 连接」里保存的那份**（见 §15）                             | 同一台 NAS 的凭据不该在设置页里填两遍；成员本来就被挡在设置页外，凭据只能由管理员维护                   |
| D-52.1 | 「上传根目录」独立可挑，**默认跟随共用连接的浏览根目录**                                                      | 目录是周报真正需要的唯一差异；默认跟随 = 不配也能用，换了连接根新文件自动跟上                           |
| D-52.2 | 没有 `report_upload_settings` 这一行 = 用默认目录，**不再等于「未配置」**                                     | 「能不能交周报」只该取决于有没有可用连接，不该再多一个必须点的「保存」                                  |
| D-52.3 | `clear-report-upload` → `reset-report-upload`，去掉恐吓式二次确认                                             | 删掉这一行只回到默认目录，不会再让全员交不了周报                                                        |
| D-53   | **上传根目录按组织配置：组织管理者改本组织，管理员可改任意组织**（见 §16）                                    | 谁的周报归谁：不同组织写在 NAS 上不同的目录，只有各组织自己清楚该写哪里；应用层的组织隔离终于落到目录上 |
| D-53.1 | 表结构：`report_upload_settings` 主键由「单行 id=1」改为 `org_id`（→ organizations，级联删除）                | 一个组织一行，读的时候天然带组织维度；组织解散即自动清掉，不留悬挂数据                                  |
| D-53.2 | **连接不变**（仍是管理员那份，D-52）：组织之间只差目录                                                        | 「目录归组织、凭据归运维」：成员与管理者都不需要碰 NAS 账号，一台 NAS 只维护一份凭据                    |
| D-53.3 | 没配目录 = 用连接的浏览根目录（**不是**「未配置」）                                                           | 少一个必填项就少一类「交不了周报」的故障；想分目录的组织自己去挑                                        |
| D-53.4 | 组织管理者提交的 `orgId` 一律忽略（用会话里的组织），与其它设置动作同一口径                                   | 越权配置在入口处就不可能发生，而不是靠一层层校验                                                        |

## 14. 实施记录（2026-09-12）

**落地的东西**（新增 6 个文件 + 改动 18 个）：

| 层        | 文件                                                                                                     | 说明                                                                                          |
| --------- | -------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------- |
| 迁移      | `server/src/db/migrations/014_report_upload_settings.sql`                                                | 单行表（`CHECK (id = 1)`）；`report_files` 不动结构                                           |
| 服务端    | `server/src/webdav/client.ts`                                                                            | 新增 `uploadIfAbsent`（PUT + `If-None-Match: *`，412 → false）与 `open`（GET 流式）           |
| 应用层    | `app/lib/report-storage.server.ts`（新）                                                                 | 命名 / 清洗 / 撞名递增 / 上传 / 下载 / 错误翻译；`stored_name` 值域 `webdav:<服务根相对路径>` |
| 应用层    | `app/lib/webdav-settings.server.ts`                                                                      | 追加「周报上传」单行配置的读写 / 视图 / 试连解析 + `UNCONFIGURED_REPORT_UPLOAD_VIEW`          |
| 应用层    | `app/lib/webdav.server.ts`                                                                               | 追加 `reportUploadClient()`（统一账号，`basePath` 固定 `/`）                                  |
| 应用层    | `app/lib/reports.server.ts`                                                                              | 归属人恒为提交者、admin 403、写 NAS、`openReportFile`（远端流式 + 老记录本地流式兜底）        |
| 路由      | `app/routes/api.reports.$id.file.$version.ts`                                                            | 改流式代理；NAS 异常映射成 502/503 + 人话                                                     |
| 页面      | `app/components/settings/report-upload-card.tsx`（新）、`webdav-tab.tsx`、`webdav-dir-picker.tsx`        | 「周报上传」卡（仅 admin）；目录选择器加 `intent` / `title` 参数以复用于统一账号              |
| 页面      | `app/routes/settings.tsx`、`app/routes/reports.tsx`                                                      | 4 个新 action（都再确认一次 admin）；上传表单去掉归属人、admin 隐藏入口                       |
| 工具      | `tools/reports-migrate-webdav.ts`（新）+ package.json 的 `reports:migrate-webdav`                        | `--dry-run` / 默认 / `--purge`；幂等 + 认领 + 失败汇总                                        |
| 工具/测试 | `tools/webdav/fake-server.ts`（新）、`tools/contract/{capture,compare,runner,fixture,smoke-ui,cases}.ts` | 公用假 WebDAV + 契约环境注入 `WEBDAV_URL` + 夹具预置配置行                                    |
| 测试      | `test/db/report-storage.test.ts`（新）、`test/webdav/client.test.ts`                                     | 存储层与搬迁 CLI 端到端（假 NAS）；客户端补条件上传与流式下载                                 |

**验收数字**（全绿）：

| 项            | 改前    | 改后                                                                                             |
| ------------- | ------- | ------------------------------------------------------------------------------------------------ |
| `test:db`     | 20      | **25**（+5：命名 / 上传 / 下载 / 未配置 503 / 搬迁 CLI）                                         |
| `test:webdav` | 9       | **13**（+4：条件上传成功与已存在、忽略条件头的服务端、流式下载、越界拒绝）                       |
| `test:api`    | 317     | **318**（+1 `reports.create.admin.forbidden`）                                                   |
| `test:auth`   | 2       | 2                                                                                                |
| `test:ui`     | 81      | **90**（+9：周报卡渲染 1 条 + 保存/清除链路 4 条 + 上传入口按身份 3 条 + 管理者看不到卡片 1 条） |
| 合计          | 429     | **448**                                                                                          |
| 迁移          | 001–013 | 001–**014**                                                                                      |
| 活表          | 18      | **19**                                                                                           |

六项门禁（`lint` / `typecheck` / `npm test` / `build` / `format:check` / `npx antd lint app`）全绿。

**与设计的偏差**（都是实施中发现的，按此为准）：

1. **迁移编号 013 → 014**：`013_task_moved_event.sql` 已占用（设计文档写 013 时按当时的目录判断有误）。
2. **`safeSegment` 是「剔除」而不是「替换」路径分隔符与控制字符**：与既有 `sanitizeName` 同一口径
   （`../etc` → `etc`，而不是 `_etc`）。
3. **下载超时单独放宽**：`open()` 至少用 120 秒。`AbortSignal.timeout` 会连同**响应体**一起掐断，
   沿用浏览目录那套 15 秒会让大文件在慢速局域网上传到一半断掉。
4. **`content-length` 取远端响应头的值**（拿不到就不带该头），不再用库里的 `size_bytes`：
   流式转发时以真实远端为准，也不会因为两者不一致而截断响应。
5. **老记录的兼容分支放在 `openReportFile` 里**，并且老记录也改成流式读（`createReadStream` + `Readable.toWeb`），
   顺带修掉原来「整份读进内存」的写法。
6. **周报页的「归属人」筛选器保留**（仅 manager / admin），`listReportOwnersFor` 继续存在但语义变成
   「筛选器数据源」；被删掉的是上传表单里的归属人下拉。
7. **页面 description 改成角色中立文案**：原文案「上传周报与总结…」含子串「上传周报」，
   会让「管理员看不到上传入口」的 SSR 断言假阳性。
8. **搬迁 CLI 的 `--purge` 修过一个真实缺陷**：回写后 `stored_name` 已是远端路径，不能再拿它拼本地路径；
   已回写的记录按旧约定还原成 `v<version><ext>`（`test/db/report-storage.test.ts` 覆盖了这条）。
9. **契约用例改名**：`reports.create.managerA.proxy` → `…managerA.own`、
   `…managerA.cross-org-owner` → `…managerA.owner-ignored`（代传取消，名字要跟着说实话）。
10. **契约环境用假 NAS**（§10 方案 A）：`tools/webdav/fake-server.ts` 由 `test:webdav`、`contract:*`、
    `smoke:ui` 共用；夹具预置 `report_upload_settings` 一行（D-52 之后改成预置管理员的 `webdav_settings` 一行），
    `startServer` 通过 `WEBDAV_URL` 注入地址。

**还没做的**（都在 `TODO.md` 的 TODO-14）：搬迁 CLI 跑完后删掉本地读分支、`UPLOADS_DIR` 只在迁移脚本里用、
补 `--pull` 反向搬迁（回滚用）。

## 15. 实施记录：配置改为「共用管理员那份连接」（2026-09-12，D-52）

### 15.1 改了什么

起因：D-46 让周报单开了一份「统一上传账号」，于是同一台 NAS 的凭据要在设置页里填两遍
（一遍给「重要文件」，一遍给周报）。团队本来共用一台 NAS，这是纯粹的重复劳动。

**改后：连接共用，只有目录是周报自己的。**

| 维度         | D-46                                   | D-52                                                         |
| ------------ | -------------------------------------- | ------------------------------------------------------------ |
| 连接从哪来   | 单行表 `report_upload_settings` 的账号 | **`webdav_settings` 里管理员那一行**（多管理员取最近保存的） |
| 卡片上的字段 | 用户名 / 密码 / 上传根目录 / 超时      | **只有上传根目录**（连接不回显——它就显示在正上方那张卡里）   |
| 默认上传目录 | `/周报`                                | **跟随共用连接的浏览根目录**                                 |
| 没配置时     | 没有那一行 = 503                       | 有管理员连接就能用；这一行只在「另挑了目录」时存在           |
| 清除配置     | `clear-report-upload`：全员交不了周报  | `reset-report-upload`：只回到默认目录，不影响可用性          |

**没有变的**（继续按 §3 / §5 / §6 执行）：落点命名 `<目录>/<用户名>/<起止日期>/<文件名>`、
`_v{n}` 与 `_2` 的撞名规则、`stored_name = webdav:<服务根相对路径>`、
`reportUploadClient()` 的 `basePath` 固定 `/`、下载流式代理、权限与组织隔离、
搬迁 CLI 的三个模式与幂等语义。

### 15.2 落地清单

| 层     | 文件                                                                                   | 说明                                                                                                                                 |
| ------ | -------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| 迁移   | `server/src/db/migrations/015_report_upload_shared_connection.sql`                     | 重建单行表：删 `username` / `password` / `timeout_ms`，`root` 默认值改为空串（= 跟随连接）；老行的 `/周报` 原样保留                  |
| 应用层 | `app/lib/webdav-settings.server.ts`                                                    | 新增 `sharedWebDavConnection()`（取管理员那份）；`resolveReportUploadConfig()` = 共用连接 + 生效目录；`save` / `reset` / 视图重写    |
| 应用层 | `app/lib/webdav.server.ts`                                                             | `reportUploadClient()` 改用共用连接；新增 `sharedConnectionConfig()` 给「测试连接 / 选择目录」                                       |
| 应用层 | `app/lib/report-storage.server.ts`                                                     | 只换提示文案（未配置的措辞从「配置周报上传」改成「保存一次连接」）                                                                   |
| 类型   | `shared/types/domain.ts`                                                               | `ReportUploadSettingsView` 改为「共用连接视图 + ownRoot / followsConnection / root」，不再有凭据字段                                 |
| 页面   | `app/components/settings/report-upload-card.tsx`                                       | 重写：**只留一个目录选择器** + 保存 / 测试连接 / 恢复默认（连接不回显，见 §4.2）                                                     |
| 路由   | `app/routes/settings.tsx`                                                              | `save-report-upload`（只收 `root`）/ `reset-report-upload` / `test-report-upload` / `browse-report-upload`（后两个用服务端共用连接） |
| 工具   | `tools/reports-migrate-webdav.ts`、`tools/contract/{fixture,cases,runner,smoke-ui}.ts` | 文案 + 夹具改预置管理员连接 + 契约用例改名 + `request.path` 归一化                                                                   |
| 测试   | `test/db/report-storage.test.ts`                                                       | 新增「默认跟随连接 / 另挑目录 / 恢复默认」用例；503 用例改成「清掉管理员连接」                                                       |

### 15.3 老数据与升级

- 迁移只重建这张小表：**已经填过的 `/周报` 原样留在 `root` 里**，升级后行为不变；
- 想回到「默认是根目录」就在卡片上点一次「恢复默认」，之后新周报跟随连接的浏览根；
- **老周报不受影响**：`stored_name` 存的是含目录的完整路径，改目录只影响新文件（§11.3 的老规矩）；
- 部署侧只要求一件事：**管理员在「WebDAV 连接」里保存过一次**（老部署通常早就保存过）。

### 15.4 验收

| 项            | 数字                                                                                                                  |
| ------------- | --------------------------------------------------------------------------------------------------------------------- |
| `test:db`     | 31 全绿（含新增的「上传根目录」用例：跟随连接 / 另挑 / 恢复默认）                                                     |
| `test:webdav` | 13 全绿                                                                                                               |
| `test:api`    | 契约 324 条全一致（golden 已按新口径重录）                                                                            |
| `test:auth`   | 2 全绿                                                                                                                |
| `test:ui`     | 99 项全绿（周报上传卡的 SSR 断言：只有上传根目录、不回显连接、不含账号密码；保存 / 恢复默认链路；管理者看不到这张卡） |
| 合计          | **469 项全绿**                                                                                                        |

六项门禁（`lint` / `typecheck` / `npm test` / `build` / `format:check` / `npx antd lint app`）全绿。

## 16. 实施记录：上传目录改为「按组织」（2026-09-12，D-53）

### 16.1 改了什么

起因：D-52 之后上传目录是**全局一份**（管理员配一次，所有人的周报写同一个目录）。
实际用法是「**不同组织的周报写在 NAS 上不同的目录里**」，而这件事只有各组织自己最清楚。

**改后：连接仍然一份（管理员的），目录一个组织一份。**

| 维度       | D-52                                  | D-53                                                            |
| ---------- | ------------------------------------- | --------------------------------------------------------------- |
| 目录作用域 | 全局一行（`id = 1`）                  | **一个组织一行**（`org_id` 主键，组织删除即级联清掉）           |
| 谁能配     | 只有管理员                            | **该组织的管理者** + 管理员（任意组织；`assertOrgManage`）      |
| 设置页     | 只有 admin 看到那张卡                 | 组织管理者与管理员都能看到；**管理员卡里可切换组织**（`?org=`） |
| 读目录     | `resolveReportUploadConfig()`（无参） | `reportUploadRootFor(orgId)`：上传按**周报所属组织**解析        |
| 下载       | 用共用连接                            | **不变**（`stored_name` 是完整路径，与组织无关）                |

**没有变的**：连接仍是管理员那份（D-52）；没配目录 = 用连接的浏览根目录（D-53.3）；
落点命名、`_v{n}` / `_2` 撞名规则、流式下载代理、搬迁 CLI 的三个模式与幂等语义（D-46）。

### 16.2 落地清单

| 层     | 文件                                                                                          | 说明                                                                                                                                                           |
| ------ | --------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 迁移   | `server/src/db/migrations/016_report_upload_settings_per_org.sql`                             | 重建表：主键 `id=1` → `org_id`（→ `organizations` 级联）；**老的全局目录复制给每个组织**，升级后落点不变                                                       |
| 应用层 | `app/lib/webdav-settings.server.ts`                                                           | `reportUploadRootFor(orgId)` / `reportUploadAvailable()` / `resolveReportUploadConfig(orgId)`；`save` / `reset` / 视图都按组织；拒绝不存在的组织与已解散的组织 |
| 应用层 | `app/lib/webdav.server.ts`                                                                    | `reportUploadClient()` → **`reportStorageClient()`**（连接级、与组织无关）；`basePath` 仍固定服务根 `/`                                                        |
| 应用层 | `app/lib/report-storage.server.ts`                                                            | `uploadReportFile` 入参加 `orgId`，目录按组织解析；下载路径不变                                                                                                |
| 应用层 | `app/lib/reports.server.ts`                                                                   | 提交用 `owner.orgId`、退回重传用 `target.orgId` 解析目录                                                                                                       |
| 类型   | `shared/types/domain.ts`                                                                      | 视图改为「orgId / orgName / connectionReady / ownRoot / followsConnection / root」，去掉连接回显字段                                                           |
| 页面   | `app/components/settings/report-upload-card.tsx`、`webdav-tab.tsx`、`app/routes/settings.tsx` | 卡片加「组织」（管理员是下拉、组织管理者只读）；管理者也能看到这张卡；`save` / `reset` 走 `assertOrgManage` 与"必须先有组织"两道校验                           |
| 工具   | `tools/reports-migrate-webdav.ts`                                                             | 逐条按 `weekly_reports.org_id` 解析根目录（CLI 与在线提交同一套规则）                                                                                          |
| 契约   | `tools/contract/{fixture,cases}.ts`                                                           | 夹具给阿尔法配 `/阿尔法`、贝塔不配 → 快照里两个组织的 `storedName` 前缀不同                                                                                    |
| 测试   | `test/db/report-storage.test.ts`                                                              | 新增「按组织解析」用例（两个组织、配与不配、恢复默认、组织不存在 / 已解散被拒）                                                                                |

### 16.3 老数据与升级

- 迁移会把**原来那个全局目录复制给每一个组织**：升级后各组织的落点与升级前完全一致；
- 不想共用的组织，由该组织管理者（或管理员）在卡片上挑自己的目录即可，**只影响之后的新文件**；
- 老周报不受影响（`stored_name` 里是含目录的完整路径，§11.3 的老规矩）。

### 16.4 验收

| 项            | 数字                                                                                                  |
| ------------- | ----------------------------------------------------------------------------------------------------- |
| `test:db`     | 31 全绿（新增「按组织解析」：两个组织各写各的、没配的用连接根、恢复默认、组织不存在 / 已解散被拒）    |
| `test:webdav` | 13 全绿                                                                                               |
| `test:api`    | 契约 324 条全一致（golden 重录：阿尔法 `/阿尔法/…`、贝塔 `/…`）                                       |
| `test:auth`   | 2 全绿                                                                                                |
| `test:ui`     | 107 项全绿（新增：管理员的组织选择器与 `?org=` 切换、组织管理者看得到并改得动本组织、改不动别的组织） |
| 合计          | **477 项全绿**                                                                                        |

六项门禁（`lint` / `typecheck` / `npm test` / `build` / `format:check` / `npx antd lint app`）全绿。
