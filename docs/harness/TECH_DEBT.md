# 技术债登记

登记规则：**每条债必须带证据**（文件 + 行为），写清影响、触发条件、修复成本；禁止把"我觉得不好"当债。
状态：`开放` / `已缓解` / `已修复`。

## 登记表

| ID      | 事项                                                | 证据                                                                                                                                                                                                                                                                                                                        | 影响                                                                                                                                                                                                                                                                                                                                                                                                | 触发条件                            | 成本                                                        | 状态                                                                                                                                                                                                                                                                          |
| ------- | --------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------- | ----------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| DEBT-01 | 无版本控制（无 git 仓库）                           | 项目根无 `.git`；`git status` 报 `not a repository`                                                                                                                                                                                                                                                                         | 改动不可回退、不可 diff/review；误删即不可恢复（本次清理只能靠手工归档兜底）                                                                                                                                                                                                                                                                                                                        | 任何一次改动                        | 0.5 天                                                      | **已修复**（2026-09-10：`git init -b main` + 基线快照 a9fa96c，后续 4 次提交）                                                                                                                                                                                                |
| DEBT-02 | `.env` 完全不生效，与 README 不符                   | `server/src/config/env.ts:21` 只读 `process.env`；`package.json` 无 `--env-file`、无 dotenv 依赖                                                                                                                                                                                                                            | 用户按 README 复制 `.env.example` 后改 `HOST/PORT/DATABASE_PATH` 全部无效，容易误判"服务起不来"                                                                                                                                                                                                                                                                                                     | 任何按文档配置环境的尝试            | 0.25 天                                                     | **已修复**（2026-09-11：`loadDotEnv()` 用 Node 内置 `process.loadEnvFile` 自动加载仓库根 `.env`；`serve` / `dev` 脚本带 `--env-file-if-exists=.env` 让 `HOST`/`PORT` 也吃到；进程环境变量优先于文件。无第三方依赖）                                                           |
| DEBT-03 | API 契约测试过薄                                    | 48 个端点 vs `test:api` 仅 2 条 test（`workbench-workflow.test.ts`、`report-workflow.test.ts` 各一条大 test 串行断言）                                                                                                                                                                                                      | 回归漏检；任何重构（含 Go 重写）都缺安全网                                                                                                                                                                                                                                                                                                                                                          | 每次改动端点逻辑                    | 1–1.5 天                                                    | **已修复**（2026-09-10：`tools/contract` 黑盒快照，`npm run contract:compare` 作为门禁；现覆盖 66 个端点 / 324 条用例 / 128 个 method+path / 17 域，正反两向自检均通过）                                                                                                      |
| DEBT-04 | `node:sqlite` 为实验特性                            | 服务启动固定打印 `ExperimentalWarning: SQLite is an experimental feature`；依赖 Node >= 22.9.0（下限由 `assertSupportedNodeRuntime` 在 `appConfig()` 里硬校验；22.9.0 是为了 `--env-file-if-exists`）                                                                                                                       | Node 升级可能带来破坏性 API 变更；无法使用更成熟的驱动（`better-sqlite3` 需原生编译）                                                                                                                                                                                                                                                                                                               | Node 大版本升级                     | 视变更而定                                                  | 开放                                                                                                                                                                                                                                                                          |
| DEBT-05 | 生产代码中存在死代码与死表                          | `server/src/db/json-migration.ts` 仅被 `test/db/json-migration.test.ts` 与其已归档的调用方引用；`server/src/db/repositories/task-repository.ts` 全仓仅被 `test/db/json-migration.test.ts` 引用（无生产调用）；`api_tokens`、`migration_runs` 两张表只被迁移/一次性迁移代码写入、无任何路由读取                              | 约 12 KB 服务端代码 + 测试属于"只能被测试触达"的死重；误导后来者以为存在 repository 分层                                                                                                                                                                                                                                                                                                            | 读代码、做架构判断时                | 0.5 天                                                      | 开放（→ TODO-04）。**范围已大幅缩小**（2026-09-10/11）：B/C 阶段删除了 `owner-token.ts` 与 4 个全仓零引用的 repository（`file`/`note`/`todo`/`user`）及 `api.users*` 3 个路由；`json-migration.ts` 属一次性 JSON→SQLite 迁移的历史产物，保留它只为让 `test:db` 的迁移用例可跑 |
| DEBT-06 | 端点文件为超长单行风格                              | `server/src/routes/workbench.ts`：39,493 B / 仅 88 行（平均每行 ~450 字符），39 个端点 + 权限判断 + SQL 全部压在一行内                                                                                                                                                                                                      | 可读性差、diff 噪音极大、漏改风险高（改一个字段可能碰掉整行）                                                                                                                                                                                                                                                                                                                                       | 每次修改该文件                      | 1–2 天                                                      | **已修复**（2026-09-10 Prettier 全量格式化：88 行 → 1,026 行）                                                                                                                                                                                                                |
| DEBT-07 | 受管启动器被移除后的能力回退                        | 2026-09-10 归档了 `scripts/start-lan.ps1`（含 Node 版本预检、5173/17500 端口冲突预检、构建后前台启动）与 `scripts/reset-owner-token.ps1`；现在只有 npm 脚本                                                                                                                                                                 | 启停前的环境校验与冲突提示消失；"双击即启动"入口消失                                                                                                                                                                                                                                                                                                                                                | 换机器、Node 版本错配、端口被占用时 | 0.5 天                                                      | 开放（→ TODO-05）。**部分缓解**（2026-09-10 Phase 4）：`npm start` 现在显式固定 `HOST=127.0.0.1 PORT=17500`，端口被占用会直接 `EADDRINUSE` 报错而不是静默换端口；Node 版本预检仍缺                                                                                            |
| DEBT-08 | 备份覆盖不全 + 备份文件散落                         | `server/src/db/backup.ts` 的 `pruneBackups` 只裁剪 `backupDirectory` 下的 `*.sqlite.bak` 且 `keep=5`；`server/src/db/client.ts:90` 生成的 `<db>.before-migration-*.bak` 落在数据库同目录（`data/` 根），不受裁剪；`data/uploads/` 内的周报文件不在任何备份范围                                                              | 迁移备份会无限累积在 `data/`；**周报正文自 D-46 起只写 NAS，本机不再是唯一副本**，剩下的风险是还没跑搬迁脚本的老式本地文件                                                                                                                                                                                                                                                                          | 迁移次数增多 / 需要恢复上传文件时   | 0.5 天                                                      | **部分缓解（2026-09-12，D-46）**：新周报正文存 NAS（由 NAS 快照/回收站负责，`REPORTS_WEBDAV.md`）；`.before-migration-*.bak` 与老式本地正文的问题仍在（→ TODO-06）                                                                                                            |
| DEBT-09 | 迁移器不校验已应用迁移的内容                        | `server/src/db/migration-runner.ts:11-28` 只记录 `version` + `applied_at`，无 SQL 内容哈希                                                                                                                                                                                                                                  | 若有人改了已应用的迁移文件，服务不会发现，导致"同一版本号、不同结构"的静默分叉                                                                                                                                                                                                                                                                                                                      | 修改历史迁移文件时                  | 0.5 天                                                      | 开放（→ TODO-07）                                                                                                                                                                                                                                                             |
| DEBT-10 | 两套数据访问风格并存                                | 旧 `routes/report.ts:7` 使用 `ReportRepository` 类；旧 `routes/workbench.ts` 全部内联 SQL（如 `findFile()` 直接写 `SELECT ... FROM important_files`），而同表的 `file-repository.ts` 无人使用                                                                                                                               | 同一张表两种访问方式，改 schema 时容易漏改一处                                                                                                                                                                                                                                                                                                                                                      | 修改数据访问层时                    | 与 DEBT-05/06 合并处理                                      | **已消除**（2026-09-10 Phase 4：两处旧路由文件已归档到 `_archive/legacy-fastify/server/routes/`，当前 `app/lib/*.server.ts` 是唯一数据访问路径）                                                                                                                              |
| DEBT-11 | 5 个空目录残留                                      | `server/src/middleware/`、`server/src/utils/`、`test/unit/`、`test/fixtures/`、`web/src/components/` 均为空                                                                                                                                                                                                                 | 误导目录结构认知（看起来有分层，实际没有）                                                                                                                                                                                                                                                                                                                                                          | 阅读目录结构时                      | 5 分钟                                                      | **已修复**（2026-09-10 Phase 4：`web/src/components/` 随 `web/` 归档；其余 4 个（含嵌套 `test/fixtures/templates/`）已删除，全仓复查无空目录）                                                                                                                                |
| DEBT-17 | 生产启动日志有模块类型警告                          | `npm run serve` 打印 `[MODULE_TYPELESS_PACKAGE_JSON] Warning: Module type of file:///…/build/server/index.js is not specified… Reparsing as ES module`                                                                                                                                                                      | 每次启动多一次重复解析（229 KB bundle，毫秒级）；日志噪音可能掩盖真正的启动告警                                                                                                                                                                                                                                                                                                                     | 每次启动生产服务                    | 0.25 天（若采纳"加 `type: module`"方案）                    | 开放（**已评估并放弃修复**：给 `package.json` 加 `"type": "module"` 会让 `server/` 在 NodeNext 下转为 ESM 语义，13 处相对导入立即报 `TS2835` 要求 `.js` 扩展名。为毫秒级收益改遍服务端导入路径不划算，见 `FULLSTACK_MIGRATION.md` Phase 4）                                   |
| DEBT-18 | `contract:compare` 启动被测服务时触发 Node 弃用告警 | `tools/contract/runner.ts:88` 用 `spawn(npmCommand, ["run", serveNpm], { shell: true })`——`shell:true` 叠加非空参数数组，Node 22.5+/24 会打印 `[DEP0190] Passing args to a child process with shell option true can lead to security vulnerabilities`                                                                       | 每次 `npm run test:api` / `contract:compare` 的日志里多一条噪音；当前参数只有受控的脚本名、无注入面                                                                                                                                                                                                                                                                                                 | 运行契约回放时                      | 0.1 天                                                      | 开放（拟改为把命令与脚本名合成单字符串 `spawn(\`${npmCommand} run ${serveNpm}\`, { shell: true })`，或显式 `cmd.exe /c`；`serveNpm` 已是受限脚本名，改法无行为风险）                                                                                                          |
| DEBT-13 | TS 7 原生编译器导致代码分析生态受限                 | `typescript@7.0.2` 的 `exports` 只有 `lib/version.cjs` 与 `typescript/unstable/*`，经典 API 已移除；实测 `typescript-eslint@8` peer 要求 `<6.1.0`                                                                                                                                                                           | 无法使用 typescript-eslint 的类型感知规则、knip / ts-prune 的全项目死代码检测；`DEBT-05` 的死代码只能靠 grep 人工核对                                                                                                                                                                                                                                                                               | 想引入新的静态分析工具时            | 受生态限制，暂无解                                          | 开放（缓解：改用不依赖 TS API 的 oxlint + Prettier；结论记入 `CODE_STYLE.md` 第 6 节）                                                                                                                                                                                        |
| DEBT-14 | `test/` 从未被 typecheck 覆盖                       | 用 `npx tsc` 对 `test/**/*.ts` 单独检查得到 **54 个类型错误**（TS2339 × 52、TS2345 × 2），集中在 `workbench-workflow.test.ts`(40) 与 `report-workflow.test.ts`(14)；根因是测试的 `request()` 辅助函数返回 Fastify `inject()` 的联合类型，叠加 `exactOptionalPropertyTypes` 后 `.json()`/`.statusCode` 无法解析              | 测试代码里的类型错误长期不可见；`tsx` 只擦除类型不做检查，所以测试照跑不误，问题被掩盖                                                                                                                                                                                                                                                                                                              | 修改测试、迁移测试时                | 0.5 天（但建议在 Phase 3 重写测试时一并解决，避免重复劳动） | **已修复**（2026-09-10：54 个错误全在 Fastify 耦合测试里，随 Phase 3 测试重写删除；剩余 4 个测试文件由新增 `test/tsconfig.test.json` + `typecheck:test` 覆盖，实测 0 错误）                                                                                                   |
| DEBT-15 | 前端单包体积随 antd 增大                            | 实测演进：antd 前 **276 KB / gzip 85 KB** → 仅基础层 **888 KB / gzip 285 KB** → 基础层+概览页 **1,328 KB / gzip 416 KB**（旧 Vite SPA，单文件无分割）。Phase 4 后 `build/client/assets` 为 **108 个文件 / 1,614 KB**，最大单块 `typography` 265 KB、`jsx-runtime` 262 KB、`table` 170 KB；服务端仍未启用压缩（按 raw 传输） | 局域网首屏加载变慢（千兆内网可接受，公网不可接受）；首次加载仍要下多个共享块                                                                                                                                                                                                                                                                                                                        | 页面与组件继续增多时                | 0.5–1 天（服务端加压缩；进一步按需拆分大块）                | 开放（**代码分割部分已解决**：2026-09-10 Phase 3 起由 React Router 8 路由级懒加载自动产出按页/按组件分块，不再是单个 1.3 MB 文件；剩余项是服务端压缩）                                                                                                                        |
| DEBT-16 | 前端缺少运行时/视觉验证手段                         | 本环境没有浏览器自动化，没有真实 DOM 渲染测试。antd 化后主要靠 `typecheck` + `oxlint` + `antd lint` + `build` 四项**静态**门禁；Phase 3 新增的 `smoke:ui` 是 **SSR 层**验证（现 98 项：登录链路 + 12 个页面是否渲染出夹具数据 + 组织隔离/角色回弹），仍然不执行客户端 JS、不校验交互与视觉                                  | antd 化引入的两处已知不确定点无法自动确认：① `Select` 的 `value=""`（「未分配」）是否正常显示 label；② `DatePicker` 在中文 locale 下的输入体验。空白页/渲染崩溃这类回归同样无人拦截（SSR 冒烟能拦"服务端渲染失败"，拦不住"水合后崩溃"）。2026-09-12 起「从企微导入」抽屉（粘贴 → 解析 → 逐条校对 → 批量导入）也在此列：SSR 只验得到页头入口按钮与 `/inbox` 跳转，抽屉内部的客户端链路没有自动化覆盖 | 每次改前端                          | 1–2 天（jsdom 挂载冒烟测试，或 Playwright + chromium）      | 开放（→ TODO-12；Phase 3/4 的静态门禁与 SSR 冒烟全绿，但**从未在浏览器里人工走查过**）                                                                                                                                                                                        |
| DEBT-12 | 前端 effect 依赖类规则暂缓                          | `react-hooks/exhaustive-deps` 与 `react/set-state-in-effect` 两条规则在 `.oxlintrc.json` 里显式关闭。旧命中点在 `web/src/pages/*`（已归档）；Phase 3 的 loader/action 写法取消了大部分"effect 内加载数据 + setState"，但规则仍保持关闭状态，未逐页复核                                                                      | 若页面里仍存在依赖数组不全的 effect，会导致数据不刷新（如搜索条件变化后不重新拉取）；改依赖会改变请求时机，属于功能改造                                                                                                                                                                                                                                                                             | 修改这些页面时                      | 1–2 天（需先补前端渲染测试）                                | 开放（→ TODO-11；规则关闭原因已在 `.oxlintrc.json` 注明，复核时先逐页确认命中是否已归零）                                                                                                                                                                                     |

## 旧 BUG_LIST 复核结论（2026-09-10 逐条对现状核实）

旧的 `BUG_LIST.md` 已随 Electron 栈归档到 `_archive/legacy-electron/BUG_LIST.md`。核对结果如下，**不要重复修已修的项**。

> 下表的「依据」列指向验证当时的文件路径，其中 `server/src/app.ts`、`test/integration/*`、`web/src/pages/*`、`web/vite.config.mts`
> 已在 Phase 4 归档到 `_archive/legacy-fastify/`；对应修复在新实现里由 `app/lib/http.server.ts`（CSP）、`app/routes/*`（页面）、
> `tools/contract/*`（契约与冒烟）承接。

| 原编号  | 问题                                                          | 现状                      | 依据                                                                                                                 |
| ------- | ------------------------------------------------------------- | ------------------------- | -------------------------------------------------------------------------------------------------------------------- |
| #1 (P0) | helmet 默认 CSP 的 `upgrade-insecure-requests` 导致局域网白屏 | ✅ 已修复                 | `server/src/app.ts:27-38` 显式移除该指令并关闭 HSTS/COOP；`test/integration/workbench-workflow.test.ts:22-26` 有断言 |
| #2 (P1) | `.env` 不生效                                                 | ✅ 已修复                 | `loadDotEnv()`（`server/src/config/env.ts`）+ `serve`/`dev` 的 `--env-file-if-exists=.env`；见 DEBT-02               |
| #3 (P1) | `start`/`start:lan` 不构建前端                                | ✅ 已修复                 | `package.json` 两个脚本均以 `npm run build &&` 开头                                                                  |
| #4 (P1) | Vite `emptyOutDir: false` 导致 dist 累积                      | ✅ 已修复                 | `web/vite.config.mts:20` 为 `emptyOutDir: true`                                                                      |
| #5 (P1) | Vite 配置以 CJS 加载 ESM 语法                                 | ✅ 已修复                 | 配置已改名为 `web/vite.config.mts`                                                                                   |
| #6 (P2) | `node`/`npm` 不在系统 PATH                                    | ⚠️ 部分缓解，且新引入回退 | npm 脚本统一用 `%npm_node_execpath%`、README 写明 NVM 步骤；但带预检的受管启动器已被移除 → DEBT-07                   |
| #7 (P2) | 通知跳转不会自动选中任务                                      | ✅ 已修复                 | `web/src/pages/WorkbenchPages.tsx:13-14` 读取 `params.get("task")` 并选中，依赖数组含 `params`                       |

**结论：7 条中 5 条已修复、1 条未修（`.env`）、1 条变成新的能力回退（启动器）。**

Phase 4 补充：`.env` 问题**仍未修**（`package.json` 里没有 `--env-file`、也没有 dotenv 依赖；根 README 已如实写明"只读进程环境变量"）。

## 度量快照（2026-09-11，账号密码 + 多组织改造完成后实测）

| 指标               | 值                                                                                                     | 备注                                                                     |
| ------------------ | ------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------ |
| 应用代码（`app/`） | 452,906 字符 / 12,438 行 / **105 个 `.ts`/`.tsx`**（13 页面路由 + 52 API 路由 + 19 lib）               | 页面 + API + SSR 全在这一层                                              |
| 服务端代码         | `server/src`（`.ts`）69,288 字符 / 1,925 行 / 19 文件                                                  | 框架无关层：db / security / config / cli / webdav / types                |
| 工具代码           | `tools/`（`.ts`）122,052 字符 / 3,145 行 / 9 文件                                                      | 契约录制、回放、夹具、SSR 冒烟、迁移演练、构建前置                       |
| 测试代码           | `test/`（`.ts`）48,475 字符 / 1,180 行 / 8 文件                                                        | 6 个数据层测试 + WebDAV 客户端 + 注册登录改密端到端                      |
| 共享类型           | `shared/types/domain.ts` 4,091 字符 / 176 行 / 1 文件                                                  | 归档后只剩这一个文件仍被引用                                             |
| 端点数             | 66（53 个 `app/routes/api.*.ts` 路由文件承载）                                                         | 契约快照覆盖 324 条用例 / 128 个 method+path / 17 域（`DEBT-03` 已修复） |
| 活表数             | 18（含 2 张死表）                                                                                      | 12 个迁移文件（001–012）                                                 |
| 依赖               | **9 个运行时依赖 + 11 个开发依赖**                                                                     | Phase 4 移除 6 个 fastify 包 + concurrently，共减 84 个包                |
| 质量门禁           | lint 0/0（143 文件）、typecheck 0（4 工程）、测试 20+9+311+2+81=**423**、构建通过、format:check 无漂移 | 另有 `npx antd lint app`：105 文件 No issues found                       |
| 构建产物           | 客户端 108 文件 / 1,614 KB；SSR bundle 500 KB                                                          | 路由级代码分割已生效（DEBT-15）                                          |
| 归档               | `_archive/legacy-electron/` + `_archive/legacy-fastify/`（20 文件，含恢复说明）                        | 两套旧实现都不参与构建与检查                                             |

## 季度体检清单

1. 跑 `npm run lint && npm run typecheck && npm test && npm run build` 与 `npm run format:check`，把结果写回 `README.md` 状态快照。
2. 本表逐条复核：能否降级、能否合并、是否已修复。
3. 检查 `data/` 体积与 `data/backups/` 份数，确认没有异常增长。
4. 复查 `PLAN.md` 的 D-05（Go 重写）与 D-06（lint 工具链）是否需要复审。
5. 复核 `.oxlintrc.json` 里被关闭的规则：能否开启、命中的存量问题是否已修（尤其 `DEBT-12`）。
