# 2026-09-02

- 完成后续重构：桌面渲染层通过本机统一 API `GET/PUT /api/v1/desktop/state` 读写业务状态，IPC 仅保留文件与令牌等桌面原生能力；共享端旧接口保持兼容。
- 完成访问令牌治理：新生成令牌采用 32 字节 base64url；桌面「助理协作」增加主人、助理、查看者令牌轮换，旧链接立即失效；启动日志不再输出令牌。
- 增加 SQLite 迁移准备：db/schema.sql 与 scripts/import-json-to-sqlite.js，成功将当前 JSON 数据验证导入 data/workbench-migrated.sqlite；历史中指向已删除助理的任务负责人会安全迁为未分配。
- 修复共享网页令牌登录遮罩无法关闭：原因是 `#loginMask` 的 `display:flex` 覆盖了 HTML `hidden` 属性；新增 `#loginMask[hidden]{display:none}`，并要求任务校验和身份校验均成功后才隐藏遮罩。SELENE 助理令牌接口验证返回 200。
